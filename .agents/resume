#!/usr/bin/env bash

set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

command -v bash >/dev/null
command -v git >/dev/null
command -v rsync >/dev/null

echo "omarchy-overrides is ready."
