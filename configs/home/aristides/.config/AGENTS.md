# Agent workflows

1. See @~/.config/agents/docs/*conventions.md for language/tool conventions I prefer.

2. Design for testability using "functional core, imperative shell": keep pure business logic separate from code that does IO.

